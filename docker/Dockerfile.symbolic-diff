# syntax=docker/dockerfile:1

ARG GO_IMAGE=golang:1.25-bookworm
FROM ${GO_IMAGE}

ARG LLVM_VERSION=16
ARG KLEE_REF=master

ENV DEBIAN_FRONTEND=noninteractive
ENV LLVM_VERSION=${LLVM_VERSION}
ENV LLVM_PREFIX=/usr/lib/llvm-${LLVM_VERSION}
ENV MLIR_DIR=/usr/lib/llvm-${LLVM_VERSION}/lib/cmake/mlir
ENV LLVM_DIR=/usr/lib/llvm-${LLVM_VERSION}/lib/cmake/llvm
ENV CMAKE_C_COMPILER=/usr/lib/llvm-${LLVM_VERSION}/bin/clang
ENV CMAKE_CXX_COMPILER=/usr/lib/llvm-${LLVM_VERSION}/bin/clang++
ENV PATH=/usr/lib/llvm-${LLVM_VERSION}/bin:/opt/klee-build/bin:${PATH}

RUN apt-get update && apt-get install -y --no-install-recommends \
    ca-certificates \
    cmake \
    curl \
    git \
    gnupg \
    lsb-release \
    ninja-build \
    pkg-config \
    python3 \
    python3-pip \
    unzip \
    wget \
    zlib1g-dev \
  && rm -rf /var/lib/apt/lists/*

RUN wget -qO /tmp/llvm.sh https://apt.llvm.org/llvm.sh \
  && chmod +x /tmp/llvm.sh \
  && /tmp/llvm.sh ${LLVM_VERSION} \
  && apt-get update && apt-get install -y --no-install-recommends \
    clang-${LLVM_VERSION} \
    lld-${LLVM_VERSION} \
    libcap-dev \
    libmlir-${LLVM_VERSION}-dev \
    libz3-dev \
    llvm-${LLVM_VERSION} \
    llvm-${LLVM_VERSION}-dev \
    llvm-${LLVM_VERSION}-tools \
    mlir-${LLVM_VERSION}-tools \
    z3 \
  && rm -rf /var/lib/apt/lists/* /tmp/llvm.sh

RUN git clone --depth 1 --branch ${KLEE_REF} https://github.com/klee/klee.git /opt/klee-src \
  && cmake -S /opt/klee-src -B /opt/klee-build -G Ninja \
    -DCMAKE_BUILD_TYPE=Release \
    -DLLVM_DIR=${LLVM_DIR} \
    -DLLVM_CONFIG_BINARY=${LLVM_PREFIX}/bin/llvm-config \
    -DLLVMCC=${LLVM_PREFIX}/bin/clang \
    -DLLVMCXX=${LLVM_PREFIX}/bin/clang++ \
    -DENABLE_SOLVER_Z3=ON \
    -DENABLE_SOLVER_STP=OFF \
    -DENABLE_POSIX_RUNTIME=OFF \
    -DENABLE_UNIT_TESTS=OFF \
    -DENABLE_SYSTEM_TESTS=OFF \
    -DENABLE_TCMALLOC=OFF \
  && cmake --build /opt/klee-build -j "$(nproc)"

COPY docker/symbolic-diff-entrypoint.sh /usr/local/bin/mlse-symbolic-diff-entrypoint
RUN chmod +x /usr/local/bin/mlse-symbolic-diff-entrypoint

WORKDIR /workspace
ENTRYPOINT ["/usr/local/bin/mlse-symbolic-diff-entrypoint"]
CMD ["bash"]
